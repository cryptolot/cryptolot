/**
 * LotteryToken configuration
 *
 * Contains deploy configuration details such as initial token amount, token
 * name, token symbol and decimal units.
 */
var totalSupply = 10000000;
var name = 'Lottery';
var symbol = 'LTRY';
var decimals = 3;

module.exports = {
  totalSupply: totalSupply,
  name: name,
  symbol: symbol,
  decimals: decimals
};
